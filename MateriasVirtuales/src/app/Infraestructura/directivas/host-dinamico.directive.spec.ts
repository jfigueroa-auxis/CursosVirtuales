import { HostDinamicoDirective } from './host-dinamico.directive';

describe('HostDinamicoDirective', () => {
  it('should create an instance', () => {
    const directive = new HostDinamicoDirective();
    expect(directive).toBeTruthy();
  });
});
