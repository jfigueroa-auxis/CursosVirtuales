import { VolverDirective } from './volver.directive';

describe('VolverDirective', () => {
  it('should create an instance', () => {
    const directive = new VolverDirective();
    expect(directive).toBeTruthy();
  });
});
