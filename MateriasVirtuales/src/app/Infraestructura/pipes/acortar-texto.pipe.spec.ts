import { AcortarTextoPipe } from './acortar-texto.pipe';

describe('AcortarTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new AcortarTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
